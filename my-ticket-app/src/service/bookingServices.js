import { rawData } from "../common/enum";

export default {
  loadListSeat: async options => {
    //call api here
    // const result = await axios.get(
    //   `${baseUrl}/${params ? `?${params}` : ""}`
    // );
    return rawData;
  }
};
